name: Setup Rust toolchain
description: Install the stable Rust toolchain (optionally with extra targets/components) and enable the cargo cache. The repository must already be checked out — local actions are resolved before any step runs, so the checkout step lives in each workflow.

inputs:
  targets:
    description: Space-separated rustup targets to install (e.g. a build target triple)
    required: false
    default: ""
  components:
    description: Space-separated rustup components to install (e.g. rustfmt clippy)
    required: false
    default: ""
  cache-key:
    description: Extra key suffix to isolate the cargo cache (e.g. per build target triple)
    required: false
    default: ""

runs:
  using: composite
  steps:
    - uses: dtolnay/rust-toolchain@stable
      with:
        targets: ${{ inputs.targets }}
        components: ${{ inputs.components }}

    - uses: Swatinem/rust-cache@v2
      with:
        key: ${{ inputs.cache-key }}